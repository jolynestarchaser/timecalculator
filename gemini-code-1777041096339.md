# Project Improvement Plan: Error Handling & UI Robustness

แผนการปรับปรุงระบบจัดการข้อผิดพลาดและการแสดงผล เพื่อป้องกันปัญหา Edge Case จากการกรอกข้อมูลเกินขีดจำกัด (Input Overflow)

## 1. Input Validation (ด่านหน้าป้องกันข้อมูล)
เป้าหมาย: จำกัดขอบเขตข้อมูลที่รับเข้ามาไม่ให้เกินความสามารถในการประมวลผลของ JavaScript

- [ ] **Implement Form Validation (Zod + React Hook Form):**
    - กำหนด Schema สำหรับ input ทุกช่อง (ค่าแรง, ราคาสินค้า)
    - ตั้งค่า `max` value (เช่น ไม่เกิน 1,000,000,000) เพื่อป้องกันตัวเลขมหาศาล
    - แสดง Error Message ทันทีที่ผู้ใช้กรอกเกินลิมิต
- [ ] **HTML Attribute Hard-limits:**
    - ใส่ `type="number"` และ `max="99999999"` ใน Input tag
    - ใช้ `onKeyPress` เพื่อบล็อกการพิมพ์ตัวอักษรที่ไม่ใช่ตัวเลข (เช่น 'e', '+', '-')

## 2. Calculation Logic & Safety (ระบบคำนวณ)
เป้าหมาย: จัดการผลลัพธ์ที่อาจจะกลายเป็น Infinity หรือ Scientific Notation

- [ ] **Safe Number Handling:**
    - ใช้ `Number.isSafeInteger()` ก่อนทำการคำนวณค่ามหาศาล
    - หากผลลัพธ์มีโอกาสเกินเซฟโซน ให้พิจารณาใช้ `BigInt` หรือ Library อย่าง `decimal.js`
- [ ] **Result Formatting:**
    - ใช้ `Intl.NumberFormat('th-TH')` เพื่อใส่ Comma ให้ตัวเลข (อ่านง่ายขึ้น)
    - ดักจับกรณีผลลัพธ์เป็น `Infinity` หรือ `NaN`:
        - หากผลลัพธ์ผิดปกติ ให้แสดงข้อความแจ้งเตือนแทนตัวเลข (เช่น "ตัวเลขสูงเกินไป")

## 3. UI/UX Defensive Design (การแสดงผลที่ยืดหยุ่น)
เป้าหมาย: ป้องกัน Layout พังเมื่อเจอชุดข้อความหรือตัวเลขที่ยาวผิดปกติ

- [ ] **CSS Overflow Fix (Tailwind CSS):**
    - เพิ่มคลาส `break-words` หรือ `break-all` ใน Container ที่แสดงผลลัพธ์
    - ใช้ `overflow-hidden` ร่วมกับ `text-ellipsis` สำหรับส่วนที่ไม่ต้องการให้ขึ้นบรรทัดใหม่
- [ ] **Dynamic Font Sizing:**
    - ปรับขนาด Font ให้เล็กลงอัตโนมัติหากจำนวนตัวเลขเพิ่มขึ้น (Responsive Text)

## 4. Application Stability (ความเสถียรภาพรวม)
เป้าหมาย: ป้องกันแอปค้างหรือหน้าขาวเมื่อเกิด Error ที่คาดไม่ถึง

- [ ] **React Error Boundaries:**
    - สร้าง Wrapper Component เพื่อดักจับ Runtime Error
    - แสดง Fallback UI ที่สวยงามแทนหน้า Error ขาวโพลนของเบราว์เซอร์
- [ ] **Toast Notifications:**
    - ติดตั้งระบบแจ้งเตือน (เช่น `react-hot-toast` หรือ `sonner`) เพื่อยิง Error Message บอกผู้ใช้เมื่อเกิดปัญหาหลังบ้าน (เช่น การเชื่อมต่อ Database หรือ API)

---
**Status:** 🏗️ Pending Implementation
**Priority:** High (Fixing UI Overflow from Tester feedback)